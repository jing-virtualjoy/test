apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm_dp.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "helm_dp.name" . }}
    helm.sh/chart: {{ include "helm_dp.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "helm_dp.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
